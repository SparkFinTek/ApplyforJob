# Place your base resume PDF here

Put a single PDF named whatever you like (e.g. `Your_Name.pdf`) in this folder,
then point `config.json -> resumes.baseResume` at the filename.

The application reads this PDF, extracts text, and uses it as the source
material when tailoring per-job resumes. Your real resume is gitignored,
so it never leaves your machine.
